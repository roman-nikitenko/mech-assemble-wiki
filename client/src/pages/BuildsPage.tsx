/** Placeholder — player builds are a future phase (needs user accounts). */
export function BuildsPage() {
  return (
    <main className="mx-auto max-w-6xl px-4 py-16 text-center">
      <h2 className="text-xl font-bold">Builds</h2>
      <p className="mt-2 text-ink-dim">
        Community builds are coming soon — this is where players will share
        their mech setups.
      </p>
    </main>
  );
}
